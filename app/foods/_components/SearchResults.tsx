import { QueryParams } from '@/types';

type Props = {
  queryParams: QueryParams;
};

export default function SearchResults({ queryParams }: Props) {
  return <div className="w-full bg-blue-300">SearchResults</div>;
}
